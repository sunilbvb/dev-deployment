# Tooltip Component (`.ui-tooltip-trigger`)

Subtle contextual bubble displaying hints or descriptions when users hover over an interactive element.

## Usage Example

Add `.ui-tooltip-trigger` class and `data-tooltip="Your tooltip content"` to the target trigger element:

```html
<button class="ui-button ui-tooltip-trigger" data-tooltip="Saves configurations to DB">
  Save
</button>
```
